{
  "name": "api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "seed": "bun run src/seed.ts",
    "gen": "bun run src/openapi.ts > openapi.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@daloyjs/core": "1.0.0-beta.1",
    "better-auth": "^1.6.20",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.0"
  }
}
